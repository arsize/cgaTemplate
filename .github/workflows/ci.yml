name: GitHub Actions Build
on:
  push:
    branches:
      - main
jobs:
    job1:
      name: template
      runs-on: "self-hosted"
      steps: 
      - name: step1
        run: |
          ls > c.txt
